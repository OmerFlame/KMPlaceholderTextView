import XCTest
@testable import KMPlaceholderTextView

final class KMPlaceholderTextViewTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(KMPlaceholderTextView().text, "Hello, World!")
    }
}
